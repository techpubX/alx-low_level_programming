#include <stdio.h>

/* len returns the length
 * *for counts
 * * Retunr the length
 * */

int main() {
	    char len[] = "omondi";
	        int i;

		    for (i = 0; len[i] != '\0'; ++i);
		        
		        printf("%d", i);
			    return 0;
}

